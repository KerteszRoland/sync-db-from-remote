version: "3.8"
services:
  db:
    image: postgres:18-alpine
    ports:
      - "5432:5432"
    env_file:
      - .env
    volumes:
      - ./sql:/docker-entrypoint-initdb.d
      - ./post-init.sql:/post-init/post-init.sql
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
